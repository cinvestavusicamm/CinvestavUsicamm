[project]
name = "ia_common"
version = "0.1.0"
description = "Shared ports and contracts for CinvestavUsicamm microservices."
requires-python = ">=3.11"

[build-system]
requires = ["hatchling>=1.13"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ia_common"]
